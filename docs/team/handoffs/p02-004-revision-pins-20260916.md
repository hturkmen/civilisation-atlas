# P02-004 — revizyon pinleri, 16 Eylül 2026

Başlangıç main: `ba572441f94b93b83103333ba278dbad53e7fa66`. Başlangıçta açık PR yoktu. Kullanıcının güncel talimatıyla PR açılmadan main'e doğrudan, normal ileri güncelleme yapılacak. Ayrı çalışma alanı: atlas-revisions. Önce kapsam özeti verildi. Yeni ücretli servis, sağlayıcı veya veri bölgesi seçilmedi.

## Tamamlanan dilim

- Gerçek beş aday için iddia hash'i, varlık içerik hash'i ve kullanılan kaynak kayıtlarının tam içerik hash'leri tek revizyon kimliğine bağlandı. Revizyon pinleri `data/editorial/revisions.json` içinde saklanır; Git geçmişi içerik geçmişini korur.
- Çözümleyici sınırın gerçek GeoJSON feature'ını doğrulanmış dosyadan okur. Yerleşim koordinat/dönemleri ve arşiv perspektifinin kayıtlı alanları da içerik hash'ine dahildir. Dosya yolu sabittir; metadata üzerinden keyfî dosya okuma yoktur.
- Yeni `evaluateRevisionPublishability` eski aday/hak kapısını koruyup buna varlık/kaynak/inceleme revizyonu eşleşmesini ekler. Eşleşmeyen veya eksik inceleme, eski geometri, kaynak değişikliği, salt genel kaynakça ve locator eksikliği olumlu sonuç vermez.
- Hazırlama komutu salt kontrol modunda eksik pin için başarısız olur; `--write` eski pinleri koruyarak yenilerini ekler ve aday onaylarını değiştirmez. Aynı içerik tekrarında yeni pin oluşmaz.
- CLI artık revizyon kapısını kullanır; yetkili inceleme servisi henüz yokken aday alanlarından sahte bir inceleme kaydı üretmez. Beş gerçek aday `unreviewed`, sıfırı yayınlanabilir.
- Kullanıcının PR gerektirmeyen teslim tercihi AGENTS.md, ekip rehberi ve README'ye işlendi.

## Kontroller

`npm test`: 53/53 (6 yeni test). Kaynak başlığı/locator/hak/revizyon notu değişikliği, gerçek geometriyi temsil eden fixture değişikliği, koordinat/eser tarihi değişikliği, eski/eksik/yanlış inceleme, bozuk pin, yalnız context/contradicts veya locator eksikliği, key order ve ilgisiz kaynak değişikliği sınandı. Olumlu onay fixture'ları yalnız test kopyalarıdır.

`node scripts/prepare-editorial-revisions.mjs`, `node scripts/validate-editorial.mjs`, `node scripts/validate-boundaries.mjs` geçti. Üretim build, typecheck ve plan kontrolünün son sonucu devam kaydına eklenecek. Önceki lockfile bağımlılıkları ve hash doğrulanmış yerel arşiv görseli kullanıldı; yeni npm ci/uzaktan görsel indirme iddiası yok.

Bu kod mevcut web bileşenleri tarafından tüketilmiyor; tarayıcıdaki koleksiyon ve UI değişmedi. Yeni tarayıcı testi ve özel demo dağıtımı gerekmiyor; yayındaki sürüm 5 korunuyor.

## Sınırlar ve devam

Bu bir yerel revizyon bağlama dilimidir; P02-004/P03-002'nin tamamlanması değildir. Postgres/PostGIS migration, DB foreign key/immutable yayın kısıtı, transaction snapshot, yetkili inceleyen kimliği ve gerçek tarihçi onayı hâlâ eksik. Hash kimliği lisans/historik doğruluk onayı ya da canlı web sayfasının arşivlendiği iddiası değildir. Perspektif asset'i burada yeniden indirilmez; kayıtlı hash pinlenir, asset baytlarını mevcut build betiği doğrular.

Sonraki somut iş: P03-002 için planlanan revizyon ilişkilerini yerel migration ve negatif referans/immutable yayın testleriyle kurmak. Production sağlayıcı/bölge/maliyet kararı öncesi yalnız yerel geliştirme; mevcut demoyu DB'ye geçirmiş sayma. Sonraki yayın adaptörü inceleme kaydını güvenilir sunucu deposundan ve tüm girdileri aynı transaction/release snapshot'ından almalıdır.

Son doğrulama: üretim build, TypeScript ve plan kontrolü geçti. Hazırlama betiği ikinci `--write` çalışmasında 0 pin ekledi (idempotent). Kod/girdiler test sonrası değişmedi; yalnız açıklama/kayıtlar tamamlandı.
