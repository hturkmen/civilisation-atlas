# Native test paketi ve kesintiden devam

Başlangıç main: `9f62333c04ba579c62935b340a28c2acf83139d3`. Önceki ayrı süreç koruması yeniden yazılmadı. Doğrudan main, PR yok.

## Uygulama

Yedi native PostgreSQL/PostGIS senaryosu: gerçek bağlantılarla claim yarışı, SKIP LOCKED, kilit sonrası heartbeat expiry, statement timeout/rollback, rollback claim, eski token reddi ve iki gerçek paket import'unda idempotency. Ayrı test-only `pg` 8.23.0 bağımlılığı ve lockfile eklendi; root/web bağımlılıkları değişmedi. Runner yalnız açıkça onaylanan boş loopback `atlas_test_*` DB kabul eder; hiçbir DB nesnesini silmez. Native sonuç yoksa skip veya başarı üretmez.

Devam kaydının başına güncel devir tablosu eklendi; eski aramayı yeniden yapmaya yönlendiren başlangıç maddesi düzeltildi. Tarihsel kayıtlar korundu, güncel tablo tek başlangıç noktasıdır.

## Doğrulama

`npm test --prefix database`: 46/46 geçti; `npm test`: 53/53 geçti; plan kontrolü geçti. Runner syntax kontrolü ve üç bağlantı-güvenlik testi geçti. Eksik config nonzero `TEST_DATABASE_ACK_REQUIRED`, yalnız sentetik localhost kapalı-port kontrolü nonzero `ECONNREFUSED` döndürdü; URL/parola loglanmadı. Son incelemede guard'ın sistem şeması filtresi gerçek `pg_` önekine daraltıldı; runner syntax/config testleri yeniden geçti. Native çalıştırma bu ortamda **engelli**: psql/postgres/docker yok. Config reddi ve bağlantı reddi yedi native senaryonun geçtiği anlamına gelmez.

## Sonraki tek iş

Native ortam varsa [hazır komutları](../../../database/NATIVE-TESTS.md) çalıştır, sürüm ve yedi sonucun çıktısını kaydet. Yoksa bu runner'ı yeniden yazma; P04-005 mobil detay paneli/odak yönetimi dilimine geç. Önce mevcut UI ve açık #22 kabul ölçütlerini kontrol et. Native çalıştırma bekleyen doğrulama olarak kalsın. P02-003/P03-002 partial; auth/RLS, bağımsız tarihçi, staging/restore, üretim scheduler açık.

Demo V5 ve özel erişim değişmedi; UI/runtime koleksiyonu değişmediği için yeni dağıtım, build/typecheck/Playwright yapılmadı. Genel proje tamamlanmadı.
